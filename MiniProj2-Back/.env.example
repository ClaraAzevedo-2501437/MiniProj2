# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/animalec

# Server Configuration
PORT=8080
HOST=0.0.0.0

# Database Bootstrap Configuration
# Set to 'true' to force reseed the database on every startup (clears existing data)
# Set to 'false' or leave empty for normal operation (only seeds if collections are empty)
FORCE_RESEED=false
